class Solution {
    public int[] searchRange(int[] nums, int target) 
    {
        int[] res={-1,-1};
    int start=0,end=nums.length;
    mid=(start+end)/2;
    while(start<=end){
        if(mid>=target)
        end=mid-1;
        else
    }
        
        
    }
}
