class Solution {
    public boolean isPalindrome(int x) {
        int rev=0,p=x,d;
        if(x<0)
        return false;

        while(p!=0)
        {
 d=p%10;
rev=rev*10+d;
p=p/10;
        }
        if(rev==x)
        return true;
        else 
        return false;
    }
}
